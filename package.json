{
  "name": "sndwch",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "parity": "node scripts/parity.mjs",
    "test": "playwright test",
    "verify": "npm run typecheck && npm run parity && npm run build && npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "typescript": "^7.0.2"
  }
}
